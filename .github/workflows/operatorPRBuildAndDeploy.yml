name: Cass Operator Build & Deploy
on: pull_request
jobs:
  build_operator_docker:
    name: Build Cass Operator Docker Image - Testing
    runs-on: ubuntu-latest
    steps:
      - name: Guarded Step
        if: github.event.pull_request.head.repo.full_name == 'datastax/cass-operator'
        run: |
          echo "This should NOT run from fork PR"
      - name: Regular Step
        run: |
          echo "This should run from fork PR"
